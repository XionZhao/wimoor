<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wimoor.erp.material.mapper.MaterialAttachmentMapper">

    <resultMap id="BaseResultMap" type="com.wimoor.erp.material.pojo.entity.MaterialAttachment">
        <id column="id" property="id"/>
        <result column="materialid" property="materialid"/>
        <result column="shopid" property="shopid"/>
        <result column="fileid" property="fileid"/>
        <result column="file_name" property="fileName"/>
        <result column="file_type" property="fileType"/>
        <result column="file_path" property="filePath"/>
        <result column="opttime" property="opttime"/>
    </resultMap>

    <select id="selectByMaterialId" resultMap="BaseResultMap">
        SELECT id, materialid, shopid, fileid, file_name, file_type, file_path, opttime
        FROM t_erp_material_attachment
        WHERE materialid = #{materialid}
        ORDER BY opttime DESC
    </select>

    <delete id="deleteByIdAndShopid">
        DELETE FROM t_erp_material_attachment
        WHERE id = #{id} AND shopid = #{shopid}
    </delete>

</mapper>
